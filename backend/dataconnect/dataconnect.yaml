specVersion: "v1"
serviceId: "radiology-assistant-912c4-service"
location: "us-east4"
schemas:
  - source: "./schema"
    datasource:
      postgresql:
        database: "radiology-assistant-912c4-database"
        cloudSql:
          instanceId: "radiology-assistant-912c4-instance"
          # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
          # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: ["./example"]
