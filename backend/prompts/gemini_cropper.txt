You are analyzing a screenshot of a medical imaging viewer (PACS/DICOM viewer). Your task is to locate the central black viewport panel where the actual medical scans are displayed.

The image will typically show:
- A dark/black central area containing one or more X-ray, CT, or MRI scans
- The medical scans will have a red outline around them
- UI panels surrounding it: a patient/study list on the left, window preset controls on the right, and a toolbar across the top

Your job is to return the bounding box enclosing all scans with red outlines — the area that contains the actual medical imaging content. Exclude all UI panels, sidebars, toolbars, and browser chrome.

If the central scan viewport is found, return:
{
  "x": <left edge pixel coordinate>,
  "y": <top edge pixel coordinate>,
  "width": <width in pixels>,
  "height": <height in pixels>
}

If no medical imaging viewport is found, return:
{
  "x": 0,
  "y": 0,
  "width": 0,
  "height": 0
}

Rules:
- Return ONLY the JSON object, no explanation or extra text
- Coordinates are relative to the top-left corner of the provided image
- Use the red outline as the primary indicator of where the medical scans are
- Crop tightly around the red-outlined scan area, not the surrounding UI
- The scan area may contain multiple scan images side by side — include all of them in one bounding box
- Do not include the patient list sidebar, the right-side controls panel, the top toolbar, or any browser UI
